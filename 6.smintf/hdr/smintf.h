#pragma once

#include <stdio.h>
/* 
   This function is tested in previos project. It's now declared and
   defined as static in source file visibilty reson
 */
const int print_integer(char *buf, int n, int radix, char *prefix);
const char* mintf(const char *format, ...);
